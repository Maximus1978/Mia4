[pytest]
markers =
    performance: performance / benchmark style tests (may be skipped by default)
    experimental: tests exercising experimental models or capabilities
    realmodel: tests that load a real GGUF model (slow)
    integration: integration-level pipeline tests
    timeout: tests with extended timeout requirement
