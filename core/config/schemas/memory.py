"""Memory module config schema placeholder (S1 stub).

Will be expanded in later sprints (vector store, retention policies, etc.).
"""
from __future__ import annotations

from pydantic import BaseModel


class MemoryConfig(BaseModel):
    # Intentionally empty placeholder to reserve namespace
    pass
