# 📋 INSTRUCTIONS

## 1) Общая цель
Создать умного, все помнящего ИИ‑ассистента, где код — это проявление поведения. Ответы краткие и фактуальные, базируются на подтверждённых данных (RAG). Система воспроизводима (venv, конфиги), наблюдаема (метрики, логи), с качественным чтением/записью в память RAG и без хардкода языка/приоритетов.

## 2) Стиль и принципы работы
- План → действия → валидация → отчёт; короткие ответы в чате.
- Всё запускается в venv; UTF‑8 по умолчанию (PYTHONUTF8=1, PYTHONIOENCODING=UTF‑8).
- Качество > скорость.
- Репродукция: единый YAML, фиксированные пороги, CI‑прогоны, отчёты в репо.
- Наблюдаемость и трассировка каждого шага; документация — SSOT.
- Выполняй задачи в логической и безопасной последовательности. Приоритет - правильное, устойчивое и расширяемое приложение в результате, а не быстрые победы и проблемы в будущем. Я тебе доверяю. Будь аккуратным, придирчивым и последовательным. Всегда держи в фокусе цель приложения. Мия должа быть умной, все помнящей и эволюционируемой в следующих итерациях.
- Обьясняй что делаешь по ходу работы на русском (ключевые вещи, каждый процесс озвучивать не нужно если это не нарушит твою эффективность и глубину рассуждений), как для UX специалиста, а не технаря.
- Каждое выполненное или запланированное задание должно проходить критерий "Как это делает Мию лучше?"
- Отчитывайся о результатах в чате по русски.
- Ты должен сам добиться максимально качественного приложения. Даже если я чтото не учитываю - предлагай, предостерегай, будь Отцом для будущей Мии. Это твой проект также как и мой.
- Важно учитывать, что все изменения должны быть задокументированы и протестированы.
- Если есть сомнения в том, что ты делаешь, или не хватает информации, спроси меня. Я помогу тебе с пониманием задачи и её контекста.
- Не допускай хардкода.
- Код не опережает спецификацию: любое поведение сперва фиксируется в документации (SSOT → реализация).
- Модели изолированы: реестр манифестов вне ядра; ядро видит только интерфейсы.
- Унификация до ядра: адаптеры приводят llama.cpp / иные рантаймы к единому контракту (generate, stream, metadata, optional embed).
- DEFAULT_COLLECTION = 'memory' до явного переопределения в конфиге.
- Конфиги только через YAML + ENV (префикс MIA__), без «магических» значений в коде.
 - Строгая модульность: никакого «общего мешка» констант в ядре; каждый модуль хранит свои настройки в собственном namespace (defaults/*.yaml или manifest) и предоставляет интерфейс через адаптер.
 - Никакого дублирования конфигурации: 1 ключ → 1 определение (Config-Registry) → использование через accessor.
 - Запрещено прошивать значения (magic numbers / строки) в коде — только через конфиг или манифест (исключение: минимальные технические дефолты ядра system/logging).
 - Отсутствие монолитных структур: RootConfig эволюционирует в агрегатор модульных схем, ядро не знает внутренних полей модулей.
 - Любое добавление ключа сначала фиксируется в `Config-Registry.md`, затем реализуется.
 - Давай однострочные команды и следи за терминалом. Учитывай что мы работаем в venv.
 - Не давай команды в формате "python - <<"PY" ...PY" они выдают ошибки.
 - После выполнения задачи или логически связанной очереди задач отмечай выполнение [x].

## 3) Цели следующего спринта: Модульный рефакторинг ядра

Главная цель: устранить монолитность RootConfig / связок между модулями и ввести модульную архитектуру (ConfigService + ModuleManager) так, чтобы замена или отключение любого немонолитного модуля (LLM, RAG, memory, perf, observability) не нарушала работу остальных.

### 3.1 Проблемы текущего состояния (summary)
- RootConfig содержит разнотипные поля (llm, perf thresholds, optional_models) — отсутствует модульное разделение схем.
- perf скрипты частично завязаны на внутренние детали провайдера (n_gpu_layers), а не на capabilities.
- Отсутствует единый жизненный цикл модулей (start/warmup/shutdown).
- Решение об optional модели зашито в factory/registry вместо отдельной стратегии.
- Нет версионирования конфигурации и мигратора.
- Смешение подсчёта метрик и инфраструктуры (perf снапшоты работают напрямую с провайдером).

### 3.2 Целевое состояние (архитектурный контур)
Core kernel:
- ConfigService: агрегация модульных схем (LLMConfig, PerfConfig, RAGConfig, MemoryConfig, ObservabilityConfig, OptionalModelsConfig).
- EventBus: уже есть — стабилизировать контракт (версионировать payload).
- ModuleManager: загрузка ModuleDescriptor (name, enabled, init()).
- ServiceRegistry (DI lookup): получение сервисов по capability.

Modules:
- llm: ModelRegistry, ModelManager (lazy load + idle sweep), Provider adapters.
- rag: Retriever + Ranker + Orchestrator (заглушка в этом спринте — интерфейсы).
- memory: VectorStoreAdapter + DocumentStore интерфейсы (заглушка).
- perf (опция): PerfCollector (подписка на события Generation* / ModelLoaded).
- observability: Metrics / StructuredLogger.
- evaluation (позже; не входит в спринт).

### 3.3 Фазы (S1–S7)
S1: Разделить Pydantic схемы (создать под‑модели; RootConfig временно агрегирует без знания полей) + schema_version.
S2: Ввести ModuleDescriptor + ModuleManager; зарегистрировать llm как первый модуль (заглушки для остальных).
S3: Вынести optional модельную логику в LLMModule (RoutingStrategy) и убрать из factory лишние решения.
S4: Capability registry: provider.info().capabilities + выбор модели по требуемой capability.
S5: GenerationResult контракт (text, usage.prompt_tokens, usage.completion_tokens, timings) + адаптация perf_probe к нему (через события, без прямых вызовов provider internals).
S6: Рефактор импорта: устранить запрещённые зависимости (llm → perf). Ввести тест на граф импортов.
S7: Обновить документацию (`Config-Registry.md`, `Perf.md`, `README`) + генератор документации конфигов из моделей.

### 3.4 Deliverables
- Новые файлы: core/modules/{llm,perf}/__init__.py, module_manager.py, config sub-schemas.
- Migration layer: если base.yaml без schema_version — автопроставить и логировать предупреждение.
- Тесты: модульная загрузка, отключение perf модуля, замена primary модели, fallback при отключённом rag.
- Скрипт проверки зависимостей (import graph) + pytest проверка.

### 3.5 KPI / Success Criteria
- Отключение perf модуля (modules.enabled без 'perf') не ломает generate/stream (тест).
- Замена primary модели на другой манифест (другая family) проходит smoke без изменения кода.
- Добавление фиктивного retrieval backend конфигом (rag.enabled=true, backend=dummy) не влияет на llm путь.
- Все perf тесты используют только публичные интерфейсы (ModelProvider + события + GenerationResult) — без прямых атрибутов провайдера.
- RootConfig (или его заменитель) не содержит прямых полей модулей — только реестр схем.

### 3.6 Out of Scope / Deferred (перенесено в backlog)
Перенесено сознательно и отражено в Perf.md / backlog:
- Flash Attention (FMHA) пересборка и сравнительные прогоны.
- Анализ аномалии n_gpu_layers=12 (глубокое профилирование).
- CI интеграция perf (переменная CI_PERF=1).
- Рекомендательная таблица конфигураций и формула T.
- cpu_baseline отдельным сценарием в perf_probe.
- Histogram latency buckets.
- Advanced resource policies (GPU memory arbitration).
- Feature flags matrix / capability negotiation расширенная (частично закладывается в S4, расширение позже).
- Encryption / ACL для RAG памяти.
- Circuit breakers / timeouts для сетевых провайдеров.

### 3.7 Риски и Митигируемые Шаги
- Риск потери совместимости YAML: решение — миграционный слой + тест legacy_base_yaml.
- Риск циклических импортов: автоматическая проверка графа в тесте.
- Риск расхождения документации Config-Registry: автогенератор + diff тест.
- Риск слома perf тестов при смене GenerationResult: сначала внедрить адаптер, затем заменить потребителей.

### 3.8 План тестов
- test_modules_enable_disable: включение/отключение perf.
- test_llm_swap_manifest: смена primary id.
- test_generation_result_contract: события содержат usage.* поля.
- test_config_migration: старый YAML → новая структура.
- test_import_graph_no_cycles: запрещённые зависимости.

### 3.9 Критерий завершения спринта
Все KPI выполнены, тесты зелёные, документация сгенерирована, backlog явно актуализирован.

### 3.10 Архив предыдущего спринта (результаты)
Старый чеклист (инфраструктура моделей + perf baseline) полностью закрыт, кроме отложенных задач (см. backlog выше). Исторический список сохранён в Git истории; удалён для упрощения текущих целей.

## Метрики оценки успеха
* p95 времени первой загрузки primary < 4s (квант Q4_K_M, локально).
* Успешная верификация checksum (100%).
* 0 невалидных ключей конфигурации в тесте.
* Генерация 20 токенов без ошибок (<1.5s на холодном старте CPU+GPU).
* Покрытие тестами core/model registry ≥ ключевые happy + 2 error cases.

## После окончания делаем:
- Рефактор конфигурации: вынести модульные схемы (per-module Pydantic), динамическая регистрация через manifest, удаление монолитного RootConfig.
- обсуждаем и потом делаем минимальный UI с заглушками. (детали на этом шаге).
- Подключить embeddings и базовый RAG pipeline (ингест → индекс → запрос) с метриками.
- Эмоциональный слой (FSM) и события EmotionShifted.
- Рефлексивный ночной пайплайн (инсайты, группировка памяти).
- Наблюдаемость: Prometheus метрики, структурированные логи, трассировка (OpenTelemetry).
- Агентный цикл (loop) с SLA и деградацией.
- Evaluation pipeline (persona adherence, rag relevance, hallucination score).
- Интеграция Vision / мультимодальности (после стабилизации базы).
- Оптимизация производительности (ExLLaMA / quant tuning / KV cache reuse).
- Сравнительный perf тест двух вариантов gpt-oss (mxfp4 vs q4_k_m) после обновления llama-cpp (проверить загрузку MXFP4 и выбрать быстрее / энергоэффективнее).